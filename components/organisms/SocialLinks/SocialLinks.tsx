import Box from '@mui/material/Box';
import { SiteData } from '../../../utils/DataParser';


function SocialLinks(data : SiteData) {

  return (
    <Box
      mt={2}
      sx={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
        

    </Box>
  );
}

export default SocialLinks;
